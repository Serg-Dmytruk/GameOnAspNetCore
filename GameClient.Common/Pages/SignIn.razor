<h3>SIGN IN</h3>

<div class="divCenter">
    @if (!waitingResponse)
    {
        <input type="text" @bind="LoginData.Login" placeholder="Login" />
        <input type="password" @bind="LoginData.Password" placeholder="Password" />
        <button class="centerOnParrentHorizontal" @onclick="SigninRequest">Login</button>
    }
    else
    {
        <div class="spinner-border centerOnParrentHorizontal" role="status">
            <span class="sr-only">Loading...</span>
        </div>
    }
</div>

@if (showErrorMess)
{
    <p class="error">Incorrect login or password</p>
}



